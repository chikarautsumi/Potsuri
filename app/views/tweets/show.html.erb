<div class="article">
    <table>
            <tr>
                <td><%= @tweet.content %></td>
            </tr>
            <tr>
                <td><%= link_to '編集', edit_tweet_path(@tweet.id) %></td>
                <td><%= link_to '削除', destroy_tweet_path(@tweet.id) %></td>
            </tr>
    </table>
</div>